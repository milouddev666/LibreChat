version: 1.3.14

cache: true

endpoints:
  custom:
    - name: "AgentRouter"
      apiKey: "${AGENTROUTER_API_KEY}"
      baseURL: "https://openrouter.ai/api/v1"
      models:
        default:
          - "nvidia/nemotron-3-ultra-550b-a55b"
        fetch: false
      titleConvo: true
      titleMethod: "completion"
      titleModel: "nemotron-3"
      modelDisplayLabel: "AgentRouter"
